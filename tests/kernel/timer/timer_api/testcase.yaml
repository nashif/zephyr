common:
  cases:
    - timer_duration_period
    - timer_period_0
    - timer_expirefn_null
    - timer_periodicity
    - timer_status_get
    - timer_status_get_anytime
    - timer_status_sync
    - timer_k_define
    - timer_user_data
tests:
  kernel.timer:
    tags: kernel
  kernel.timer.tickless:
    build_only: true
    extra_args: CONF_FILE="prj_tickless.conf"
    arch_exclude: riscv32 nios2 posix
    tags: kernel
