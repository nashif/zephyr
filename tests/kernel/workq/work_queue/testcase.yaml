common:
  cases:
    - sequence
    - resubmit
    - delayed
    - delayed_resubmit
    - delayed_resubmit_thread
    - delayed_cancel
tests:
  kernel.workqueue:
    tags: core bat_commit
  kernel.workqueue.poll:
    tags: core bat_commit
    extra_configs:
      - CONFIG_POLL=y
