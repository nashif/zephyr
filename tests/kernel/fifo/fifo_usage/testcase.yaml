common:
  cases:
    - single_fifo_play
    - dual_fifo_play
    - isr_fifo_play
tests:
  kernel.fifo.usage:
    tags: kernel
  kernel.fifo.usage.poll:
    extra_args: CONF_FILE="prj_poll.conf"
    tags: kernel
