tests:
  drivers.dma.cyclic:
    required_snippets:
      - testing
    depends_on: dma
    tags:
      - drivers
      - dma
    platform_allow:
      - xmc47_relax_kit
      - xmc45_relax_kit
    filter: dt_nodelabel_enabled("tst_dma0")
