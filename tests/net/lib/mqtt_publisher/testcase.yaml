common:
  cases:
    - mqtt_init
    - mqtt_connect
    - mqtt_pingreq
    - mqtt_publish
    - mqtt_disconnect
common:
  tags: net mqtt
  harness: net
  depends_on: netif
tests:
  net.mqtt:
    min_ram: 16
  net.mqtt.tls:
    platform_whitelist: frdm_k64f qemu_x86
    extra_args: CONF_FILE="prj_tls.conf"
